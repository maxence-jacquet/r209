{% extends 'base.html.twig' %}

{% block title %}Détail Note{% endblock %}

{% block body %}
  <div class="card shadow-lg">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h3 class="mb-0">📝 Détail de la Note</h3>
      <div>
        <a href="{{ path('app_note_index') }}" class="btn btn-outline-secondary me-2">
          <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
        <a href="{{ path('app_note_edit', {'id': note.id}) }}" class="btn btn-primary me-2">
          <i class="bi bi-pencil"></i> Modifier
        </a>
        {{ include('note/_delete_form.html.twig') }}
      </div>
    </div>

    <div class="card-body p-0">
      <table class="table mb-0 table-striped table-hover">
        <tbody>
          <tr>
            <th scope="row">Id</th>
            <td>{{ note.id }}</td>
          </tr>
          <tr>
            <th scope="row">Titre</th>
            <td>{{ note.titre }}</td>
          </tr>
          <tr>
            <th scope="row">Description</th>
            <td>{{ note.description }}</td>
          </tr>
          <tr>
            <th scope="row">Création</th>
            <td>{{ note.creat ? note.creat|date('Y-m-d H:i:s') : '' }}</td>
          </tr>
          <tr>
            <th scope="row">Fin</th>
            <td>{{ note.endAt ? note.endAt|date('Y-m-d H:i:s') : '' }}</td>
          </tr>
          <tr>
            <th scope="row">Propriété</th>
            <td>{{ note.propriete }}</td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
{% endblock %}
